# Replace only the existing <Proxy "balancer://l0-engine"> block.
# Keep its public ProxyPass, <Location> limits, and RequestHeader unchanged.
# If the coordinator is unavailable, Apache falls back to the existing engines.
<Proxy "balancer://l0-engine">
    BalancerMember "http://127.0.0.1:18768" retry=1 connectiontimeout=2 timeout=900
    BalancerMember "http://127.0.0.1:18767" status=+H retry=1 connectiontimeout=2 timeout=900
    BalancerMember "http://127.0.0.1:28767" status=+H retry=1 connectiontimeout=2 timeout=900
    ProxySet lbmethod=byrequests
</Proxy>
